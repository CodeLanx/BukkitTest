name: ${project.name}
version: ${project.version}
description: ${project.description}
author: You!
website: http://codelanx.com/
main: ${project.groupId}.${project.artifactId}.${project.name}
prefix: Testing